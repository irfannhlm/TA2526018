<!DOCTYPE html>
<html lang="id">
  <head>
    <title>Login System</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap"
      rel="stylesheet"
    />

    <style>
      :root {
        --bg-color: #ffe6f2;
        --primary-pink: #ff80ab;
        --dark-pink: #d81b60;
        --text-dark: #4a4a4a;
        --shadow-soft: 0 10px 30px rgba(255, 105, 180, 0.2);
      }

      body {
        background-color: var(--bg-color);
        font-family: "Poppins", sans-serif;
        color: var(--text-dark);
        height: 100vh;
        overflow: hidden;
        position: relative;
      }

      /* Dekorasi Background */
      .circle-decoration {
        position: absolute;
        border-radius: 50%;
        background: linear-gradient(135deg, #ff80ab, #f06292);
        opacity: 0.1;
        z-index: -1;
      }
      .circle-1 {
        width: 300px;
        height: 300px;
        top: -50px;
        right: -50px;
      }
      .circle-2 {
        width: 200px;
        height: 200px;
        bottom: 50px;
        left: -50px;
      }

      .card-login {
        border: none;
        border-radius: 25px; /* Rounded besar sesuai tema dashboard */
        box-shadow: var(--shadow-soft);
        background: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(10px);
        padding: 40px;
        width: 400px;
      }

      .login-logo {
        height: 100px; /* Atur tinggi logo sesuai kebutuhan */
        width: auto;
        object-fit: contain;
        margin-bottom: 15px;
      }

      .form-control {
        background-color: #fff0f6; /* Pink sangat muda */
        border: 1px solid transparent;
        border-radius: 0 15px 15px 0; /* Rounded kanan */
        padding: 12px;
        font-size: 0.95rem;
      }
      .form-control:focus {
        background-color: #fff;
        border-color: var(--primary-pink);
        box-shadow: none;
      }

      .input-group-text {
        background-color: #fff0f6;
        border: none;
        border-radius: 15px 0 0 15px; /* Rounded kiri */
        color: var(--primary-pink);
        padding-left: 20px;
      }

      /* Efek fokus untuk seluruh grup input */
      .input-group:focus-within .input-group-text {
        background-color: #fff;
        border: 1px solid var(--primary-pink);
        border-right: none;
      }
      .input-group:focus-within .form-control {
        border-left: none;
      }

      .btn-rose {
        background: linear-gradient(135deg, #ff80ab, #d81b60);
        color: white;
        border: none;
        padding: 12px;
        font-weight: 600;
        border-radius: 50px;
        transition: all 0.3s ease;
        box-shadow: 0 5px 15px rgba(216, 27, 96, 0.3);
      }
      .btn-rose:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(216, 27, 96, 0.4);
        color: white;
      }
    </style>
  </head>
  <body class="d-flex justify-content-center align-items-center">
    <div class="circle-decoration circle-1"></div>
    <div class="circle-decoration circle-2"></div>

    <div class="card card-login">
      <div class="text-center mb-4">
        <img src="/images/logo.png" alt="Logo Aplikasi" class="login-logo" />
        <h4 class="fw-bold" style="color: var(--text-dark)">
          Monitoring Diskusi
        </h4>
        <p class="text-muted small">Masuk untuk mengelola perangkat</p>
      </div>

      <% if (error) { %>
      <div
        class="alert alert-danger py-2 small border-0 bg-danger bg-opacity-10 text-danger mb-4 rounded-3 text-center"
      >
        <i class="fas fa-exclamation-circle me-1"></i> <%= error %>
      </div>
      <% } %>

      <form action="/login" method="POST">
        <div class="mb-3">
          <div class="input-group">
            <span class="input-group-text"><i class="fas fa-user"></i></span>
            <input
              type="text"
              name="username"
              class="form-control"
              placeholder="Username"
              required
            />
          </div>
        </div>
        <div class="mb-4">
          <div class="input-group">
            <span class="input-group-text"><i class="fas fa-lock"></i></span>
            <input
              type="password"
              name="password"
              class="form-control"
              placeholder="Password"
              required
            />
          </div>
        </div>
        <button type="submit" class="btn btn-rose w-100">
          SIGN IN <i class="fas fa-arrow-right ms-2"></i>
        </button>
      </form>

      <div class="text-center mt-4">
        <small class="text-muted" style="font-size: 0.75rem"
          >© TA252601018</small
        >
      </div>
    </div>
  </body>
</html>
