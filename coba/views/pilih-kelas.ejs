<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pilih Kelas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --bg-color: #ffe6f2;
            --card-bg: #ffffff;
            --primary-pink: #ff80ab;
            --dark-pink: #d81b60;
            --text-dark: #4a4a4a;
            --text-muted: #888888;
            --shadow-soft: 0 10px 30px rgba(255, 105, 180, 0.15);
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--bg-color);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
            color: var(--text-dark);
        }

        .login-card {
            background: var(--card-bg);
            border-radius: 25px;
            padding: 40px;
            box-shadow: var(--shadow-soft);
            width: 100%;
            max-width: 500px;
            text-align: center;
            animation: fadeIn 0.5s ease-out;
        }

        .brand-logo {
            width: 80px;
            height: 80px;
            background-color: #fff0f6;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px auto;
            color: var(--dark-pink);
            font-size: 2rem;
        }

        h2 {
            font-weight: 700;
            color: var(--text-dark);
            margin-bottom: 5px;
        }

        .role-badge {
            background-color: var(--primary-pink);
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 30px;
            display: inline-block;
        }

        .class-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr); /* 2 Kolom */
            gap: 15px;
            margin-bottom: 30px;
        }

        .class-btn {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 20px;
            background: white;
            border: 2px solid #fce4ec; /* Pink sangat muda */
            border-radius: 20px;
            text-decoration: none;
            color: var(--dark-pink);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .class-btn i {
            font-size: 1.5rem;
            margin-bottom: 10px;
            color: var(--primary-pink);
            transition: color 0.3s;
        }

        .class-btn span {
            font-weight: 600;
            font-size: 1.1rem;
        }

        .class-btn:hover {
            background: linear-gradient(135deg, #ff80ab, #f06292);
            color: white;
            border-color: transparent;
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(240, 98, 146, 0.4);
        }

        .class-btn:hover i {
            color: white;
        }

        .btn-logout {
            color: var(--text-muted);
            text-decoration: none;
            font-size: 0.9rem;
            transition: 0.3s;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-logout:hover {
            color: var(--dark-pink);
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>

    <div class="login-card">
        <div class="brand-logo">
            <i class="fas fa-university"></i>
        </div>

        <h2>Selamat Datang</h2>
        <div class="role-badge"><%= role %> Mode</div>
        
        <p class="text-muted mb-4">Silakan pilih kelas untuk memulai sesi:</p>

        <div class="class-grid">
            <% classes.forEach(cls => { %>
                <a href="/<%= role %>?kelas=<%= cls %>" class="class-btn">
                    <i class="fas fa-chalkboard-teacher"></i>
                    <span><%= cls %></span>
                </a>
            <% }) %>
        </div>

        <div class="border-top pt-3 mt-2">
            <a href="/" class="btn-logout">
                <i class="fas fa-sign-out-alt"></i> Logout / Ganti Akun
            </a>
        </div>
    </div>

</body>
</html>